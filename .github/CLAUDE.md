# .github/
> L2 | 父级: ../CLAUDE.md

成员清单
workflows/windows.yml: 推 main 后打 exe，挂到 Release tag windows（SportDesk-Windows.zip）

本机 macOS 不能交叉编译 Windows。推到 GitHub 后等 Actions 绿勾，Releases 的 windows 标签下 zip；国内可走镜像，不要用 Actions Artifacts。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
